# Instructions

## Installation:
1. open new terminal and run "npm install" when inside the root folder.
2. run "npm i express cors" too.
3. run "npm i express-session" as well.

## Running the app:
1. open new terminal and run "server/index.js"
2. open a second terminal and run "npm run dev"